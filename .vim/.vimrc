syntax enable

" Setting the line number
set number

" Setting the color scheme
colorscheme sift 

" Setting the tabstops as actual spaces
set tabstop=2
set softtabstop=0
set expandtab
set shiftwidth=2
set smarttab

" Setting the prediction
set wildmenu

" Setting for highlighted searches
set hlsearch

" Setting case insensitive searches
set incsearch

" Setting for displaying cursor position
set ruler

" Setting for always displaying status line
set laststatus=2

" Setting for always showing the command input
set showcmd


